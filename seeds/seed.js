const sequelize = require("../config/connection");
//const { User } = require("../models");

// const users = [
//     {

//     }
// ]


// const seedMe = async () => {
//     await sequelize.sync({ force: true });
//     const seeedUsers = await User.bulkCreate(users, {
//       individualHooks: true,
//     });
    //Create more seed here before exit
    //==============
//     process.exit(0);
//   };